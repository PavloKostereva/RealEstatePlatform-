# Real Estate Platform

Повнофункціональна веб-платформа для оренди та купівлі нерухомості, побудована на Next.js 14 з App Router, TypeScript та Tailwind CSS.

## Функціональність

### Для користувачів
- 🔐 Аутентифікація та реєстрація
- 👤 Профіль користувача з редагуванням даних, аватара та ролі
- 📋 Створення, редагування та видалення оголошень
- 🔍 Пошук оголошень з фільтрами (тип, категорія, ціна, площа, кімнати)
- 📍 Карта з кластерингом оголошень
- 💾 Збереження улюблених оголошень (wishlist)
- 💬 Повідомлення між користувачами
- ⭐ Відгуки та рейтинги

### Для адміністраторів
- 📊 Статистика платформи (графіки)
- 👥 Управління користувачами
- 📝 Модерація оголошень
- ✅ Підтвердження ролі власника (owner)

## Технологічний стек

- **Frontend**: Next.js 14 (App Router), React 18, TypeScript
- **Styling**: Tailwind CSS
- **Database**: PostgreSQL з Prisma ORM
- **Authentication**: NextAuth.js
- **Maps**: Mapbox GL JS
- **Charts**: Recharts
- **File Upload**: Multer (вбудовано в Next.js API routes)

## Встановлення

### 1. Клонуйте репозиторій

```bash
git clone <repository-url>
cd Kursova
```

### 2. Встановіть залежності

```bash
npm install
```

### 3. Налаштуйте змінні оточення

Створіть файл `.env` на основі `.env.example`:

```env
DATABASE_URL="postgresql://user:password@localhost:5432/real_estate?schema=public"
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here-change-in-production
NEXT_PUBLIC_MAPBOX_TOKEN=your-mapbox-token-here
```

### 4. Налаштуйте базу даних

```bash
# Генеруйте Prisma Client
npm run db:generate

# Застосуйте міграції
npm run db:migrate

# Або використайте db:push для розробки
npm run db:push
```

### 5. Запустіть сервер розробки

```bash
npm run dev
```

Відкрийте [http://localhost:3000](http://localhost:3000) у браузері.

## Структура проєкту

```
Kursova/
├── app/                    # Next.js App Router
│   ├── api/               # API routes
│   ├── auth/              # Сторінки аутентифікації
│   ├── admin/             # Адмін-панель
│   ├── profile/           # Профіль користувача
│   ├── listings/          # Оголошення
│   ├── map/               # Карта
│   └── layout.tsx         # Основний layout
├── components/            # React компоненти
│   ├── admin/             # Компоненти адмін-панелі
│   ├── layout/            # Навігація, футер
│   ├── listings/          # Компоненти оголошень
│   ├── map/               # Компоненти карти
│   ├── profile/           # Компоненти профілю
│   └── search/            # Компоненти пошуку
├── lib/                   # Утиліти та налаштування
│   ├── auth.ts            # NextAuth конфігурація
│   └── prisma.ts          # Prisma Client
├── prisma/                # Prisma схема
│   └── schema.prisma      # Моделі БД
└── public/                # Статичні файли
    └── uploads/           # Завантажені зображення
```

## Моделі бази даних

- **User**: Користувачі системи (USER, OWNER, ADMIN)
- **Listing**: Оголошення нерухомості
- **SavedListing**: Збережені оголошення
- **Message**: Повідомлення між користувачами
- **Review**: Відгуки та рейтинги

## API Endpoints

### Аутентифікація
- `POST /api/auth/signup` - Реєстрація
- `POST /api/auth/[...nextauth]` - NextAuth endpoints

### Оголошення
- `GET /api/listings` - Список оголошень (з фільтрами)
- `GET /api/listings/[id]` - Деталі оголошення
- `POST /api/listings` - Створити оголошення
- `PUT /api/listings/[id]` - Оновити оголошення
- `DELETE /api/listings/[id]` - Видалити оголошення

### Профіль
- `GET /api/users/[id]` - Отримати користувача
- `PUT /api/users/profile` - Оновити профіль
- `POST /api/users/[id]/verify-owner` - Підтвердити власника (адмін)

### Збережені оголошення
- `GET /api/saved` - Список збережених
- `POST /api/saved/[id]` - Зберегти оголошення
- `DELETE /api/saved/[id]` - Видалити зі збережених

### Повідомлення
- `GET /api/messages` - Список повідомлень
- `POST /api/messages` - Створити повідомлення
- `PUT /api/messages/[id]` - Оновити повідомлення

### Відгуки
- `POST /api/reviews` - Створити/оновити відгук

### Адмін
- `GET /api/admin/stats` - Статистика
- `GET /api/admin/users` - Список користувачів
- `GET /api/admin/listings` - Список оголошень (всі)
- `POST /api/admin/listings/[id]/approve` - Підтвердити оголошення

### Завантаження
- `POST /api/upload` - Завантажити зображення

## Тестування

```bash
# Запуск тестів (якщо додані)
npm test
```

## Розгортання

### Vercel (рекомендовано)

1. Підключіть репозиторій до Vercel
2. Додайте змінні оточення в налаштуваннях
3. Виконайте міграції БД

### Інші платформи

```bash
npm run build
npm start
```

## Ліцензія

MIT

## Автор

Ваше ім'я






